inteiro funcao_teste(inteiro: n)
  se (x > y) então
    se (y < x) então
      retorna(x + y)
    senão
        se (1 > 2) então
          retorna(x + y)
        fim
      retorna(x - y)
    fim
  senão
    se (x > x) então
      retorna(x * y)
    senão
      retorna(x / y)
    fim
  fim
  retorna(res)
fim